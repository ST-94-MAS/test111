スティッキーセッション（セッション永続性）は、クライアントが特定のサーバーに継続的に接続するようにする機能

通常、セッション情報はクッキーに保存され、次回以降のリクエスト時にクライアントは同じサーバーに接続される。

EdgeとChromeとは別のブラウザなので、Edgeが送信するリクエストにはChromeのセッション情報が含まれていません。そのため、ALBはEdgeからのリクエストを新しいセッションとして扱う。

ALBのスティッキーセッションについて:https://docs.aws.amazon.com/ja_jp/elasticloadbalancing/latest/application/sticky-sessions.html
ブラウザのセッション管理：


WEBサーバはGIPを持っていないので、IPアドレスに対して外部から別々にアクセスすることは出来ない。しかしALBの振り分け設定で同様のことが可能と思われ、例えばこのURLにアクセスした場合はWEBサーバ1に、このURLにアクセスした場合はWEBサーバ2に、ということは出来ると思われる
