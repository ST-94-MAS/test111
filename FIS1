EC2はポート25（SMTP標準ポート）を使用したメール送信には制限がかかっています。
この制限を解除するには、AWSサポートにリクエストする必要があります。
Postfixを使用する場合はポート25の制限を回避するために、一般的にはSMTPの代替ポートである587または465を使用することが推奨されます。
https://aws.amazon.com/jp/blogs/messaging-and-targeting/amazon-ses-smtp-recommendations/
