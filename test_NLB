vpc	
clientRoutingPolicy?	
crossZoneEnabled?	
deletionProtection?	
denyAllIgwTraffic?	
enforceSecurityGroupInboundRulesOnPrivateLinkTraffic?	
internetFacing?	
ipAddressType?	
loadBalancerName?	
securityGroups?	
vpcSubnets?	


The VPC network to place the load balancer in.
The AZ affinity routing policy.
Indicates whether cross-zone load balancing is enabled.
Indicates whether deletion protection is enabled.
Indicates whether the load balancer blocks traffic through the Internet Gateway (IGW).
Indicates whether to evaluate inbound security group rules for traffic sent to a Network Load Balancer through AWS PrivateLink.
Whether the load balancer has an internet-routable address.
The type of IP addresses to use.
Name of the load balancer.
Security groups to associate with this load balancer.
Which subnets place the load balancer in.

VPCネットワーク（vpc）: NLBを配置するVPCネットワークを指定します。これはNLBが動作する基盤となるネットワーク環境です。

AZアフィニティルーティングポリシー（clientRoutingPolicy）: オプションで、AZ（アベイラビリティゾーン）に基づくルーティングポリシーを指定します。これは通常、NLBのトラフィック配分に関連する設定です。

クロスゾーンロードバランシングの有効化（crossZoneEnabled）: クロスゾーンロードバランシングを有効にするかどうかを指定します。有効にすると、NLBは複数のAZにまたがるインスタンスにトラフィックを均等に分散します。

削除保護の有効化（deletionProtection）: NLBを誤って削除するのを防ぐための削除保護を有効にするかどうかを指定します。

IGWを通じたトラフィックのブロック（denyAllIgwTraffic）: インターネットゲートウェイ（IGW）を通じたトラフィックをNLBがブロックするかどうかを指定します。

PrivateLinkトラフィックに対するセキュリティグループルールの評価（enforceSecurityGroupInboundRulesOnPrivateLinkTraffic）: AWS PrivateLinkを通じてNLBに送信されたトラフィックに対して、インバウンドセキュリティグループルールを評価するかどうかを指定します。

インターネット向けアドレスの有無（internetFacing）: NLBがインターネットからアクセス可能なアドレスを持つかどうか（インターネット向けか内部向けか）を指定します。

IPアドレスタイプ（ipAddressType）: 使用するIPアドレスのタイプ（例：IPv4、IPv6）を指定します。

ロードバランサー名（loadBalancerName）: NLBの名前を指定します。

セキュリティグループ（securityGroups）: NLBに関連付けるセキュリティグループを指定します。セキュリティグループは、トラフィックのフィルタリングルールを提供します。

配置するサブネット（vpcSubnets）: NLBを配置するサブネットを指定します。これにより、NLBが動作するネットワークの範囲を決定します。
