既存リソースの取得はconstructの中で実施することでよろしいでしょうか。Stackから渡したほうが汎用性の高いconstructになるのではと考えます。

  constructor(scope: Construct, id: string, props: NetworkProps) {
    super(scope, id);

    const myNlbSg = new ec2.SecurityGroup(this, "NlbSg2", {
      vpc: props.vpc,
      allowAllOutbound: true,
      securityGroupName: `${props.systemPrefix}-${props.envPrefix}-Sg-nlb2`,
    });
    myNlbSg.addIngressRule(
      ec2.Peer.anyIpv4(),
      ec2.Port.tcp(80),
    );
    myNlbSg.addIngressRule(
      ec2.Peer.anyIpv4(),
      ec2.Port.tcp(8080),
    );
